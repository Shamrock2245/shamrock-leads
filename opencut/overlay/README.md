# Shamrock OpenCut overlay

Copied onto `opencut-classic` after `git clone` in `Dockerfile`.

Includes bundled SFX, visual effects, clip-to-clip transitions, text presets,
and the Auto/AI panel. Source of truth is `opencut-classic`. Recopy this
overlay before `docker compose --profile edit build`.
