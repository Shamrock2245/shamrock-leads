fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
httpx>=0.27.0
maigret>=0.6.0
sherlock-project>=0.15.0
# Phone registration checks (Instagram / Snapchat / Amazon) — does not alert target
ignorant>=1.2
# Email registration checks (120+ sites incl. Instagram) — does not alert target
holehe>=1.61
trio>=0.22.0
termcolor>=2.0.0
beautifulsoup4>=4.12.0
# Instagram username enrichment (email/phone) — requires INSTAGRAM_SESSION_ID cookie
toutatis>=1.31
phonenumbers>=8.13.0
pycountry>=22.0.0
requests>=2.28.0
# SpiderFoot is optional — no usable spiderfoot>=4 on PyPI (only a stub).
# runners.py already degrades when missing. Install later only if needed.
