fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
httpx>=0.27.0
maigret>=0.6.0
sherlock-project>=0.15.0
# SpiderFoot is optional — no usable spiderfoot>=4 on PyPI (only a stub).
# runners.py already degrades when missing. Install later only if needed.
