# ML Model Storage
# Trained models are saved here as .joblib files with companion _meta.json.
# These files are auto-generated by scoring/model_trainer.py and should NOT
# be committed to git (they're environment-specific binary artifacts).
*.joblib
*_meta.json
