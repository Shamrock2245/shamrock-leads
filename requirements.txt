# ShamrockLeads — Python Dependencies
# Core
requests>=2.31.0
pymongo[srv]>=4.6.0
python-dotenv>=1.0.0
APScheduler>=3.10.4

# Google Sheets (optional legacy writer) + Gmail/Calendar API
gspread>=6.0.0
google-auth>=2.25.0
google-auth-oauthlib>=0.8.0
google-api-python-client>=2.100.0

# AI / Scoring (future)
openai>=1.12.0

# Slack notifications
slack-sdk>=3.27.0

# Scraper dependencies — Collier (curl_cffi), Charlotte (patchright+xvfb), Hendry (DrissionPage)
curl-cffi>=0.7.0
beautifulsoup4>=4.12.0
DrissionPage>=4.0.0
patchright>=1.50.0  # Charlotte County — Cloudflare Turnstile bypass (also run: patchright install chromium)

# Selenium — Seminole County (NorthPointe Suite Angular portal)
selenium>=4.15.0
webdriver-manager>=4.0.0

# PDF parsing — Citrus County (jail roster is PDF)
pdfplumber>=0.10.0

# Dashboard — Quart (async Flask replacement) + Hypercorn ASGI server
quart>=0.19.0
quart-cors>=0.7.0
hypercorn>=0.17.0
motor>=3.3.0                # Async MongoDB driver for Quart
httpx>=0.27.0               # Async HTTP client (replaces requests in async handlers)
flask>=3.0.0                # Legacy — kept for backward compat import only
PyYAML>=6.0                 # Blog auto-publisher YAML frontmatter parsing

# Active Bonds PDF generation & date parsing
reportlab>=4.0.0
python-dateutil>=2.8.0
PyMuPDF>=1.24.0

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.0
