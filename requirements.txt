# ShamrockLeads — Python Dependencies
# Core
requests>=2.31.0
pymongo[srv]>=4.6.0
python-dotenv>=1.0.0
APScheduler>=3.10.4

# Google Sheets (optional legacy writer)
gspread>=6.0.0
google-auth>=2.25.0

# AI / Scoring (future)
openai>=1.12.0

# Slack notifications
slack-sdk>=3.27.0

# Scraper dependencies — Collier (curl_cffi), Charlotte & Hendry (DrissionPage)
curl-cffi>=0.7.0
beautifulsoup4>=4.12.0
DrissionPage>=4.0.0

# Dashboard
flask>=3.0.0

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.0
