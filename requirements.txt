# ShamrockLeads — Python Dependencies
# Core
requests>=2.31.0
pymongo[srv]>=4.6.0
python-dotenv>=1.0.0
APScheduler>=3.10.4

# Google Sheets (optional legacy writer) + Gmail/Calendar API
gspread>=6.0.0
google-auth>=2.25.0
google-auth-oauthlib>=0.8.0
google-api-python-client>=2.100.0

# AI / Scoring (future)
openai>=1.12.0

# ML / Predictive Scoring Pipeline
# NOTE: Do NOT add xgboost to the Docker image — its Linux wheel pulls
# nvidia-nccl-cu12 (~300MB CUDA) and has blown the Hetzner volume mid-deploy.
# model_trainer already falls back to RandomForest when xgboost is absent.
# Optional on a roomy host:  pip install 'xgboost>=2.0.0'
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
joblib>=1.3.0

# Slack notifications
slack-sdk>=3.27.0

# Scraper dependencies — DrissionPage (legacy), curl_cffi (TLS impersonation), Playwright (Obscura CDP)
curl-cffi>=0.7.0
beautifulsoup4>=4.12.0
DrissionPage>=4.0.0
patchright>=1.50.0  # Charlotte County — Cloudflare Turnstile bypass (also run: patchright install chromium)
playwright>=1.48.0  # Obscura CDP connection for CF-protected counties (Charlotte, Manatee)
# CAPTCHA OCR stack (see scrapers/captcha_ocr.py)
# Core (light, always install):
ddddocr>=1.4.0      # Captcha-specialized local OCR (JailTracker, etc.)
Pillow>=10.0.0      # Preprocess variants for captcha_ocr
# System: install tesseract-ocr binary (Dockerfile + brew) — used via CLI, no Python pkg
# Optional heavy engines (do NOT add to slim Docker image unless volume allows):
#   pip install 'paddlepaddle>=2.6.0' 'paddleocr>=2.7.0'   # production-grade
#   pip install 'easyocr>=1.7.0'                            # PyTorch OCR
# Surya is for document layout OCR, not tiny captchas — skip for this stack.

# Selenium — Seminole County (NorthPointe Suite Angular portal)
selenium>=4.15.0
webdriver-manager>=4.0.0

# PDF parsing — Citrus County (jail roster is PDF)
pdfplumber>=0.10.0

# Dashboard — FastAPI + Uvicorn ASGI server (migrated from Quart)
fastapi>=0.115.0,<0.120.0
starlette>=0.37.0,<0.41.0     # Pin with FastAPI — avoid Starlette 1.x on_startup breakage
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0          # Server-Sent Events for /api/events/stream
motor>=3.3.0                  # Async MongoDB driver
httpx[socks]>=0.27.0            # Async HTTP client + SOCKS proxy (Hillsborough scraper)
cryptography>=43.0.0          # Fernet symmetric encryption (OAuth token storage)
PyJWT>=2.8.0                  # JWT state tokens for OAuth CSRF protection
aiohttp>=3.9.0                # Async HTTP client for contact discovery OSINT pipeline
python-multipart>=0.0.9       # Required by FastAPI for form data / file uploads
itsdangerous>=2.1.2           # Signed-cookie state for dashboard PIN auth
PyYAML>=6.0                   # Blog auto-publisher YAML frontmatter parsing

# Active Bonds PDF generation & date parsing
reportlab>=4.0.0
fpdf2>=2.7.0                  # OSINT PDF export (dashboard/services/osint_pdf_export.py)
python-dateutil>=2.8.0
PyMuPDF>=1.24.0

# Official OSI / Palmetto bond & discharge reports (XLSX)
openpyxl>=3.1.0

# Firebase Admin SDK (BlueBubbles URL sync via Firestore)
firebase-admin>=6.4.0

# Court Intelligence (Juriscraper + CourtListener SE US pipeline)
juriscraper>=2.6.0

# Phonetic name matching (Metaphone, Soundex, Jaro-Winkler)
jellyfish>=1.0.0

# reCAPTCHA v2 audio solver (free, self-hosted — Hillsborough County)
pydub>=0.25.1      # MP3→WAV conversion (requires ffmpeg system package)
SpeechRecognition>=3.10.0  # Google Speech Recognition (free, no API key)

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.0
nodriver>=0.38  # Seminole, Lake — JS-rendered jail portals (headless Chrome via CDP)
